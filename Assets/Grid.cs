using System;
using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class Grid:MonoBehaviour
{
    // [SerializeField] private int width;
    //
    // [SerializeField] private int height;
    //
    // [SerializeField] private int cellSize;
    // [SerializeField] private int[,] gridArray;
    //
    // public Grid(int width, int height, int cellSize)
    // {
    //     this.width = width;
    //     this.height = height;
    //     this.cellSize = cellSize;
    //     
    //     gridArray=new int[width, height];
    //     Debug.Log("aas");
    //     for (int x = 0; x < gridArray.GetLength(0); x++)
    //     {
    //         for (int y = 0; y < gridArray.GetLength(1); y++)
    //         {
    //             Debug.DrawLine(GetWorldPosition(x,y),GetWorldPosition(x,y+1),Color.blue);
    //             Debug.DrawLine(GetWorldPosition(x,y),GetWorldPosition(x+1,y),Color.blue);
    //             //Debug.Log(x+" "+y);
    //         }
    //     }
    // }
    //
    // private Vector3 GetWorldPosition(int x, int y)
    // {
    //     return new Vector3(x, y) * cellSize;
    // }

    // [SerializeField] private int xMax = 160;
    // [SerializeField] private int yMax = 160;
    // [SerializeField] private int cellSize = 20;
    // private void Start()
    // {
    //     coor
    //     startPoint = new coordinates
    // }
//}
}
